{
  "name": "logstream-ai",
  "version": "1.0.0",
  "description": "High-Throughput Log Ingestion & Observability Platform",
  "scripts": {
    "install:all": "cd ingestion-api && npm install && cd ../log-worker && npm install && cd ../dashboard && npm install",
    "dev": "concurrently \"cd ingestion-api && npm run dev\" \"cd log-worker && npm run dev\" \"cd dashboard && npm run dev\"",
    "start:all": "npm run dev",
    "traffic": "node traffic-simulator.js"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "axios": "^1.7.0",
    "redis-commander": "^0.9.0"
  }
}
